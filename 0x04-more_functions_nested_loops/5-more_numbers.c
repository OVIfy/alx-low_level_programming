#include "main.h"
/**
* more_numbers - checks if input is a digit
* Return: returns 1 if c is digit else 0
*/

void more_numbers(void)
{
	int i = 0;
	
	while (++i <= 10)
	{
	for (i = '0'; i <= '14'; i++)
		_putchar(i);
	_putchar('\n');
	}
}
